services:
  app:
    build: .
    restart: unless-stopped
    environment:
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
    volumes:
      - ./data:/app/data
    networks:
      - pitchcraft_default

networks:
  pitchcraft_default:
    external: true
